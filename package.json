{
  "name": "yoc-test-task",
  "version": "1.0.0",
  "description": "yoc ad video loading task",
  "scripts": {
    "ts": "tsc -w",
    "serve": "cd public && web-dev-server --node-resolve --watch --open --app-index index.html",
    "dev": "npm run ts & npm run serve",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@web/dev-server": "^0.4.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
